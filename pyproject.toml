[project]
name = "cruchcount"
version = "0.1.0"
description = "Offline snack-store checkout app built with PyQt6."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyqt6>=6.8.0",
]
[tool.uv]
index = [
    { url = "https://mirrors.aliyun.com/pypi/simple/", default = true }
]